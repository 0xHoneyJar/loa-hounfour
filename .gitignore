# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
*.log

# Production builds
build/
dist/
out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and Editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS
Thumbs.db

# Package manager
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Temporary files
tmp/
temp/
*.tmp

# =============================================================================
# Loa Framework — Ephemeral Development Tooling
# =============================================================================
# The Loa framework mounts on demand via scripts/mount-loa.sh
# and leaves no trace in the repository when not in use.

# System Zone — framework skills, hooks, scripts, protocols
.claude/

# Runtime State — sprint execution, audit logs, simstim
.run/

# Task Tracking — beads SQLite database
.beads/

# Grimoire State — framework memory, analytics, a2a comms
# (Protocol knowledge migrated to docs/)
grimoires/

# Framework Config — session state, not project config
.loa.config.yaml
.loa-version.json
.loa-setup-complete
.loa/

# Semantic search state
.ck/

# =============================================================================
# Eval Framework
# =============================================================================
evals/results/*
!evals/results/.gitkeep
# Track config files inside eval fixtures (needed for CI)
!evals/fixtures/**/.loa.config.yaml

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.0xhoneyjar.com/loa-hounfour/8.7.0/reputation-state-changed-payload",
  "additionalProperties": false,
  "description": "Payload emitted when a personality's reputation state machine transitions between states (cold / warming / established / authoritative).",
  "type": "object",
  "required": [
    "personality_id",
    "collection_id",
    "pool_id",
    "from_state",
    "to_state",
    "trigger",
    "blended_score"
  ],
  "properties": {
    "personality_id": {
      "minLength": 1,
      "type": "string"
    },
    "collection_id": {
      "minLength": 1,
      "type": "string"
    },
    "pool_id": {
      "minLength": 1,
      "type": "string"
    },
    "from_state": {
      "description": "Reputation state-machine state: cold → warming → established → authoritative. Transitions are protocol-fixed.",
      "anyOf": [
        {
          "const": "cold",
          "type": "string"
        },
        {
          "const": "warming",
          "type": "string"
        },
        {
          "const": "established",
          "type": "string"
        },
        {
          "const": "authoritative",
          "type": "string"
        }
      ]
    },
    "to_state": {
      "description": "Reputation state-machine state: cold → warming → established → authoritative. Transitions are protocol-fixed.",
      "anyOf": [
        {
          "const": "cold",
          "type": "string"
        },
        {
          "const": "warming",
          "type": "string"
        },
        {
          "const": "established",
          "type": "string"
        },
        {
          "const": "authoritative",
          "type": "string"
        }
      ]
    },
    "trigger": {
      "minLength": 1,
      "type": "string"
    },
    "blended_score": {
      "minimum": 0,
      "maximum": 1,
      "type": "number"
    }
  },
  "$comment": "contract_version=8.7.0, min_supported=6.0.0"
}

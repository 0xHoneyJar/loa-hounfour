{
  "schema_id": "billing-entry",
  "expression_version": "2.0",
  "constraints": [
    {
      "id": "billing-pricing-provenance",
      "expression": "source_completion_id == null || pricing_snapshot != null",
      "description": "If billing references a completion, pricing snapshot must be present",
      "severity": "warning",
      "fields": ["source_completion_id", "pricing_snapshot"],
      "type_signature": {
        "source_completion_id": "string",
        "pricing_snapshot": "object"
      }
    },
    {
      "id": "billing-reconciliation-delta",
      "expression": "reconciliation_delta_micro == null || reconciliation_mode == 'provider_invoice_authoritative'",
      "description": "Reconciliation delta only valid with provider_invoice_authoritative mode",
      "severity": "warning",
      "fields": ["reconciliation_delta_micro", "reconciliation_mode"],
      "type_signature": {
        "reconciliation_delta_micro": "bigint_coercible",
        "reconciliation_mode": "string"
      }
    }
  ]
}
